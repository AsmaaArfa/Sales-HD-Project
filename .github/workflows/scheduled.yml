# name: Scheduled — Daily Pipeline Run

# on:
#   schedule:
#     - cron: "0 3 * * *"    # 03:00 UTC daily (after Airflow ingestion at 02:00)
#   workflow_dispatch:
#     inputs:
#       execution_date:
#         description: "Execution date (YYYY-MM-DD), defaults to yesterday"
#         required: false

# jobs:
#   trigger-dataform:
#     name: Trigger Dataform Workflow
#     runs-on: ubuntu-latest
#     environment: production
#     steps:
#       - uses: actions/checkout@v4

#       - name: Authenticate to GCP
#         uses: google-github-actions/auth@v2
#         with:
#           credentials_json: ${{ secrets.GCP_SA_KEY }}

#       - name: Set up Python
#         uses: actions/setup-python@v5
#         with:
#           python-version: "3.11"

#       - name: Install dependencies
#         run: pip install google-cloud-dataform google-auth

#       - name: Determine execution date
#         id: date
#         run: |
#           if [ -n "${{ github.event.inputs.execution_date }}" ]; then
#             DATE="${{ github.event.inputs.execution_date }}"
#           else
#             DATE=$(date -u -d "yesterday" +%Y-%m-%d 2>/dev/null || date -u -v-1d +%Y-%m-%d)
#           fi
#           echo "execution_date=$DATE" >> $GITHUB_OUTPUT
#           echo "Running for date: $DATE"

#       - name: Trigger Dataform workflow
#         env:
#           GCP_PROJECT_ID: ${{ secrets.GCP_PROJECT_ID_PROD }}
#           DATAFORM_REPO: ${{ secrets.DATAFORM_REPO }}
#           DATAFORM_REGION: ${{ secrets.DATAFORM_REGION }}
#           EXECUTION_DATE: ${{ steps.date.outputs.execution_date }}
#         run: |
#           python3 scripts/trigger_dataform.py \
#             --project "$GCP_PROJECT_ID" \
#             --region "$DATAFORM_REGION" \
#             --repo "$DATAFORM_REPO" \
#             --vars "execution_date=$EXECUTION_DATE" \
#             --wait

#       - name: Run quality checks
#         env:
#           GCP_PROJECT_ID: ${{ secrets.GCP_PROJECT_ID_PROD }}
#         run: python3 scripts/run_quality_checks.py

#       - name: Write job summary
#         if: always()
#         run: |
#           echo "## Daily Pipeline Run" >> $GITHUB_STEP_SUMMARY
#           echo "- **Date**: ${{ steps.date.outputs.execution_date }}" >> $GITHUB_STEP_SUMMARY
#           echo "- **Triggered by**: ${{ github.event_name }}" >> $GITHUB_STEP_SUMMARY
#           echo "- **Status**: ${{ job.status }}" >> $GITHUB_STEP_SUMMARY
